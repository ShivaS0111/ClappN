-- Historical baseline. Pre-Flyway schema was created via dumps / Hibernate ddl-auto.
-- Existing databases that already have version 1 in flyway_schema_history will not re-run this.
-- If checksum validation fails after restore, run: mvn flyway:repair
SELECT 1;
